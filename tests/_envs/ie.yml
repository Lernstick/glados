# `ie` environment config goes here
modules:
    config:
        WebDriver:
            host: %WINDOWS_HOST%
            port: %IE_PORT%
            url: http://%GLADOS_HOST%/glados/
            browser: 'internet explorer'
            capabilities:
                se:ieOptions:
                    ignoreProtectedModeSettings: true # seems not to work
