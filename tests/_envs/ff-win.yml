# `win` environment config goes here
modules:
    config:
        WebDriver:
            host: %WINDOWS_HOST%
            port: %FF_PORT%
            url: http://%GLADOS_HOST%/glados/
            browser: 'firefox'
            capabilities:
                moz:firefoxOptions:
                    args: ["-headless"]
