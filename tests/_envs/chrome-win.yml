# `chrome` environment config goes here
modules:
    config:
        WebDriver:
            host: %WINDOWS_HOST%
            port: %CHROME_PORT%
            url: http://%GLADOS_HOST%/glados/
            browser: 'chrome'
            capabilities:
                chromeOptions:
                    args: ["--headless", "--disable-gpu", "--no-sandbox"]
